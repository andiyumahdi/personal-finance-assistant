# OS
.DS_Store
Thumbs.db

# Editors
.vscode/
.idea/

# Root-level catch-all (backend/ and frontend/ have their own, more specific
# .gitignore files - this covers anything at the repo root only)
*.log

node_modules/
# Supabase CLI local state (created by `supabase link` / `supabase migration new`)
supabase/.temp/
supabase/.branches/

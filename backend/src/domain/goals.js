export async function createGoal(userId, data) {
  // TODO: insert goal { title, target_amount, deadline }
}

export async function updateGoalProgress(goalId, amount) {
  // TODO: update current_saved, flip status to 'achieved' if target reached
}

// Login page. Auth is Google OAuth via NextAuth, bound to a WhatsApp
// phone number through a one-time link token. See docs/SPECIFICATION.md
// section 2.5 (Dashboard Account Linking).

export default function LoginPage() {
  // TODO: read link token from query string
  // TODO: trigger Google OAuth sign-in via next-auth
  return <div>Login page placeholder</div>;
}

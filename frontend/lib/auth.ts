// Shared NextAuth configuration (Google provider) used by the route handler
// in app/api/auth/[...nextauth]/route.ts. See docs/SPECIFICATION.md section 2.5.

export const authOptions = {
  // TODO: providers: [GoogleProvider(...)]
  // TODO: callbacks to bind google_id -> phone_number via link_token
};

// Chart library (e.g. Recharts) not yet installed - add only when this
// component is actually implemented (see spec: don't install unused deps).

export default function CategoryChart() {
  // TODO: category breakdown chart
  return <div>CategoryChart placeholder</div>;
}
